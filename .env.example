# Optional: set these or pass via CLI/API
# PG_DB2_GIT_SYNC_REPO_PATH=/path/to/satish chauhan-database-migration
# PG_DB2_GIT_SYNC_TARGET_DDL_BASE=target_ddl
# PG_DB2_GIT_SYNC_DEFAULT_ENV=non_prod
# Install in editable mode
pip install -e .

# Install with dev deps and run tests
pip install -e ".[dev]"
pytest

pg-db2-git-sync --version
pg-db2-git-sync discover "C:\Users\chauhs\Desktop\program\github\satish chauhan-database-migration" --env non_prod --db-name D_ACO